name: VSCode CI

on:
  push:
    branches: ["main"]
    paths:
      - "extensions/vscode/**"
      - ".github/workflows/vscode-ci.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "extensions/vscode/**"
      - ".github/workflows/vscode-ci.yml"

env:
  NODE_OPTIONS: --no-deprecation

jobs:
  compile:
    name: Test + package
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: extensions/vscode
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: "24"

      - name: Install
        run: npm ci || npm install

      - name: Package (.vsix, runs tests + prepublish build)
        run: npm run package

      - name: Upload .vsix artifact
        uses: actions/upload-artifact@v7
        with:
          name: peridot-vscode-vsix
          path: extensions/vscode/*.vsix
          if-no-files-found: error
